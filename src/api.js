const ip = 'localhost';

export const getURL = `http://${ip}:8900`;
export const postURL = `http://${ip}:8080`;
export const URL = {
  putLike: `http://${ip}:8082/api/v1/like`,
  getMyFeeds: `http://${ip}:8080/api/v1/user/feed/1`
}